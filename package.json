{
  "name": "@warhol/config-file",
  "version": "1.4.0",
  "main": "dist/cjs/index.js",
  "types": "dist/types/index.d.ts",
  "author": "peter@peterkroener.de",
  "license": "MIT",
  "scripts": {
    "ts:lint": "tslint -p .",
    "ts:build": "rm -rf dist/ && tsc -p tsconfig.json",
    "test": "ts-node node_modules/.bin/jasmine 'test/*.test.ts'",
    "test:dev": "nodemon --watch src --watch test -e ts --exec 'yarn test'",
    "build": "yarn ts:build",
    "prepublishOnly": "yarn test && yarn build"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/jasmine": "^2.8.9",
    "@warhol/tsconfig": "^1.0.0",
    "ajv": "^6.5.4",
    "better-ajv-errors": "^0.6.1",
    "jasmine": "^3.3.0",
    "nodemon": "^1.18.5",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "typescript": "^3.1.4"
  }
}
